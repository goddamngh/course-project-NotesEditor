<Window x:Class="NoteEditor.UI.ChoiceCategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NoteEditor.UI"
        mc:Ignorable="d"
        xmlns:converters="clr-namespace:NoteEditor.UI.Converters"
        
        Title="NoteEditor.ChoiceCategoryWindow"
        Style="{StaticResource StyleWindow}"
        WindowState="Maximized">


    <Window.Resources>
        <converters:DrawingToMediaColorConverter x:Key="ColorConverter"/>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Все категории:"/>

        <DataGrid Grid.Row="1"
                  x:Name="CategoryList"
                  Background="White"
                  IsReadOnly="True"
                  SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="False"
                  CanUserResizeColumns="True"
                  CanUserResizeRows="False"
                  CanUserSortColumns="True"
                  RowHeaderWidth="0"
                  HorizontalAlignment="Center"
                  SelectionChanged="CategoryList_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название"
                            Binding="{Binding Name}"
                            Width="*"/>
                <DataGridTemplateColumn Header="Цвет" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Rectangle Width="Auto" Height="Auto" Margin="2" ToolTip="{Binding Color.Name}">
                                <Rectangle.Fill>
                                    <SolidColorBrush Color="{Binding Color, 
                                                     Converter={StaticResource ColorConverter}}"/>
                                </Rectangle.Fill>
                            </Rectangle>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Content="Выбрать" 
                    x:Name="ChoiceButton" 
                    Click="ChoiceButton_Click"
                    Grid.Column="0"
                    IsEnabled="False"/>
            <Button Content="Создать новую" 
                    x:Name="AddNewCategoryButton" 
                    Click="AddNewCategoryButton_Click"
                    Grid.Column="1"/>
            <Button Content="Редактировать" 
                    x:Name="ChangeCategoryButton" 
                    Click="ChangeCategoryButton_Click"
                    Grid.Column="2"
                    IsEnabled="False"/>
            <Button Content="Удалить" 
                    x:Name="DeleteCategoryButton" 
                    Click="DeleteCategoryButton_Click"
                    Grid.Column="3"
                    IsEnabled="False"/>
            <Button Content="Отмена" 
                    x:Name="CancelButton" 
                    Click="CancelButton_Click"
                    Grid.Column="4"/>
        </Grid>
    </Grid>
</Window>
