<Window x:Class="NoteEditor.UI.AuthorizationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NoteEditor.UI"
        mc:Ignorable="d"

        WindowStartupLocation="CenterScreen"
        Title="NoteEditor.AuthorizationWindow"
        Style="{StaticResource StyleWindow}"
        Height="230"
        Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Авторизация/регистрация"
                   Grid.Row="0"
                   Grid.ColumnSpan="2"/>

        <TextBlock Text="Имя пользователя:"
                   Grid.Row="1"
                   Grid.Column="0"
                   VerticalAlignment="Center"/>
        <TextBox x:Name="UserNameTextBox"
                 Grid.Row="1"
                 Grid.Column="1"/>

        <TextBlock Text="Пароль:"
                   Grid.Row="2"
                   Grid.Column="0"
                   VerticalAlignment="Center"/>
        <PasswordBox x:Name="PasswordBox"
                     Grid.Row="2"
                     Grid.Column="1"
                     Margin="10"
                     BorderBrush="Black"
                     BorderThickness="1"
                     Background="WhiteSmoke"/>

        <Button Grid.Row="6"
                Grid.Column="1"
                x:Name="CreateAccountButton"
                Content="Создать аккаунт"
                Click="CreateAccountButton_Click"/>

        <Button Grid.Row="6"
                Grid.Column="0"
                x:Name="EnterButton"
                Content="Войти"
                Click="EnterButton_Click"/>

    </Grid>
</Window>
