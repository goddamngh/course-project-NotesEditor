<Window x:Class="NoteEditor.UI.AccountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NoteEditor.UI"
        mc:Ignorable="d"
        xmlns:converters="clr-namespace:NoteEditor.UI.Converters"
        
        WindowStartupLocation="CenterScreen"
        Title="NoteEditor.AccountWindow"
        Style="{StaticResource StyleWindow}"
        Height="478"
        Width="Auto">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Аккаунт"/>
        
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="10">
            <TextBlock Text="Имя пользователя: "
                       HorizontalAlignment="Left"/>
            <TextBlock Text="{Binding Username}"
                       HorizontalAlignment="Left"/>
        </StackPanel>
        
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    Margin="10">
            <TextBlock Text="Статус VIP: "
                       HorizontalAlignment="Left"/>
            <TextBlock x:Name="IsVipTextBlock"
                       HorizontalAlignment="Left"/>
        </StackPanel>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    Margin="10">
            <TextBlock Text="Дата регистрации: "
                       HorizontalAlignment="Left"/>
            <TextBlock Text="{Binding RegistrationDate}"
                       HorizontalAlignment="Left"/>
        </StackPanel>

        <Button Grid.Row="4"
                Content="Купить Подписку"
                x:Name="BuyVipButton"
                Click="BuyVipButton_Click"
                Margin="10,0,10,10"/>
        
        <Button Grid.Row="5"
                Content="Выйти из аккаунта"
                x:Name="ExitButton"
                Click="ExitButton_Click"
                Margin="10,0,10,10"/>

        <Button Grid.Row="6"
                Content="Удалить аккаунт"
                x:Name="DeleteAccountButton"
                Click="DeleteAccountButton_Click"
                Margin="10,0,10,10"/>

        <Button Grid.Row="7"
                Content="Назад"
                x:Name="CancelButton"
                Click="CancelButton_Click"
                Margin="10,0,10,10"/>

    </Grid>
</Window>
